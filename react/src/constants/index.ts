export const PATH = {
    ANY: "*",
    ROOT: "/",
    LOGIN: "/login",
    REGISTER: "/register",
    USER: "/user",
    DASHBOARD: "/dashboard",
};

export const API_PATH = {
    LOGIN: "/login",
    REGISTER: "/register",
    USER: "/user",
};

export enum LOCAL_STORAGE_KEY {
    ACCESS_TOKEN = "ACCESS_TOKEN",
}
